apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: media
  namespace: jellyfin
spec:
  accessModes:
    - ReadOnce
  resources:
    requests:
      storage: 10Gi # actually meaningless for NFS
  volumeName: media