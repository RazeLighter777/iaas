apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: media
  namespace: jellyfin
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: 10Gi # actually meaningless for NFS
  volumeName: media