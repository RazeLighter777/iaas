apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: config-pvc
  name: config-pvc
  namespace: pihole
spec:
  storageClassName: freenas-iscsi-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
