apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: config-pvc
  name: config-pvc
  namespace: pihole
spec:
  storageClassName: truenas-iscsi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
