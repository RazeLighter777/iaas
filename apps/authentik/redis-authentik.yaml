---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis-authentik
  namespace: authentik
spec:
  interval: 5m
  releaseName: redis
  chartRef:
    kind: OCIRepository
    name: redis
    namespace: redis
  values:
    redis:
      usePassword: true