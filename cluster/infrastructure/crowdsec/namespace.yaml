apiVersion: v1
kind: Namespace
metadata:
  name: crowdsec
  labels:
    app.kubernetes.io/component: security
    # Agent DaemonSet hostPath-mounts /var/log -- blocked by `baseline`.
    pod-security.kubernetes.io/enforce: privileged
