apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No top-level namespace -- istio-integration/ resources live in istio-system,
# and every other manifest declares its own namespace explicitly.
resources:
  - ./lapi
  - ./bouncers
  - ./istio-integration
