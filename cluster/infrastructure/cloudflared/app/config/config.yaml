---
originRequest: {}

ingress:
  - hostname: "${domain_name}"
    service: &svc https://istio-ingressgateway.istio-system.svc.cluster.local:443
  - hostname: "*.${domain_name}"
    service: *svc
  - service: http_status:404
