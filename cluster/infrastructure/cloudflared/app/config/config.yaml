---
originRequest: {}

ingress:
  - hostname: "${domain_name}"
    service: &svc http://istio-ingressgateway.istio-system.svc.cluster.local:80
  - hostname: "*.${domain_name}"
    service: *svc
  - service: http_status:404
