---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - apps
  # Common policies are deployed per-namespace as needed
  # They are not included here to avoid namespace conflicts
