apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn-fast-unreplicated-strictlocal
  resources:
    requests:
      storage: 200Gi