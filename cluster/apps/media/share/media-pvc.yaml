apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn-fast-unreplicated
  resources:
    requests:
      storage: 200Gi