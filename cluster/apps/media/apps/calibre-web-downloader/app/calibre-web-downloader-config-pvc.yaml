apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-web-downloader-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: longhorn-fast
