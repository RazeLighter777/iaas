---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: pod-gateway
  namespace: flux-system
spec:
  path: "./cluster/apps/pod-gateway/app"
  targetNamespace: pod-gateway
  interval: 1h
  retryInterval: 3m
  timeout: 60s
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  postBuild:
    substituteFrom:
      - kind: Secret
        name: cluster-settings
  