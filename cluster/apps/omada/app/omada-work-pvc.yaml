apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: omada-work
  namespace: omada
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn-fast
