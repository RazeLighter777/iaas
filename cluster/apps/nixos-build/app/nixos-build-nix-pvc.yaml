apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nixos-build-nix
  namespace: nixos-build
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi
  storageClassName: longhorn-fast
